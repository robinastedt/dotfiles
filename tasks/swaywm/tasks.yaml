---
- name: 'SwayWM: Install packages'
  become: true
  ansible.builtin.dnf:
    name:
      - '@swaywm-extended'
      - grimshot
    state: latest
  register: swaywm_packages

- name: 'SwayWM: Remove incompatible packages'
  become: true
  ansible.builtin.dnf:
    name:
      - volumeicon
    state: removed

- name: 'SwayWM: Remove obsolete configuration files'
  file:
    path: '{{ xdg_config_home }}/{{ item }}'
    state: absent
  with_items:
    - sway/config
    - i3
    - i3status

- name: 'SwayWM: Create directories'
  file:
    path: '{{ xdg_config_home }}/{{ item }}'
    state: directory
  with_items:
    - sway
    - sway/config.d

- name: 'SwayWM: Copy configuration files'
  copy:
    src: 'files/config/sway/config.d/{{ item }}'
    dest: '{{ xdg_config_home }}/sway/config.d/{{ item }}'
  register: swaywm_config
  with_items:
    - keyboard.conf
    - monitor.conf
    - system-menu.conf

- name: 'SwayWM: Restart sway'
  shell: swaymsg reload
  when:
    swaywm_packages.changed or swaywm_config.changed
...
